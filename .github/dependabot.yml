version: 2
updates:
  # The package has zero runtime dependencies, so only GitHub Actions are tracked.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
